функција фибоначијевНиз (н) {
    ако (н === 1) {
        врати [0, 1]
    } у супрoтном {
        дај да с = фибоначијевНиз(н - 1)
        с.гурни(с[с.дужина - 1] + с[с.дужина - 2]);
        врати с
    }
}

испиши(фибоначијевНиз(8).споји(', '))